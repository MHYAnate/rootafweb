// app/offline/layout.tsx

export const metadata = {
  title: 'Offline',
  description: 'no vex,  no get network.',
};

export default function OfflineLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}